# 工具：update_project_config

## 用途

根据用户的自然语言说明更新创作配置，更新结果随本轮对话持久化。

## 参数

- `message`（字符串，必填）：用户对配置的调整说明，例如“改成36集，每集3分钟，取第5到80章”。

## 返回

更新后的完整配置字典（字段同 get_project_config）。

## 使用时机

仅当用户明确要求调整集数、单集时长、原著范围、平台规格、风格或付费策略时调用；调用后必须把更新后的配置完整复述给用户确认，不要擅自更改用户未提及的字段。