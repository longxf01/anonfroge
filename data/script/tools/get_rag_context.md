# 工具：get_rag_context

## 用途

读取本轮剧本创作的 RAG 检索上下文，获取系统已按用户消息召回的项目资料。

## 参数

无参数。

## 返回

- `text`：已编排的召回资料全文，可直接引用；
- `runtime`：检索运行时诊断（检索模式、命中数、索引状态等）；
- `hits`：命中明细列表，每项含来源标识、来源类型、标题、得分、片段文本与元数据。

## 使用时机

回答涉及小说章节、人物、剧情、设定或视觉风格的问题时，若系统注入的资料不足，调用本工具获取完整命中明细；不要在未读取资料的情况下凭记忆作答。